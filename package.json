{
  "name": "bitizen-inpage",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./dist && parcel build --no-source-maps src/index.js"
  },
  "dependencies": {
    "eth-json-rpc-http": "git+https://github.com/BitizenWallet/eth-json-rpc-http.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "browserslist": [
    "since 2017-06"
  ]
}
